#include <iostream>
#include <bits/stdc++.h>
#include <atcoder/all>
using namespace atcoder;
using namespace std;
typedef long long ll;
#define rep(i,n) for(int i=0; i<(n); i++)
const long long INF = numeric_limits<long long>::max();

// g++ template.cpp -o template.out
// ./template.out <input> output

int main(){
    while(1){
        int n;
        cin >> n;
        if(n == 0) break;
    }
}